#!/usr/bin/env bash
set -euo pipefail
. "$(cd "$(dirname "$0")" && pwd)/_docker_helpers.sh"
# prefer project drush if present
exec run_in_php sh -lc '[ -x vendor/bin/drush ] && exec vendor/bin/drush "$@" || exec drush "$@"' -- "$@"

