#!/usr/bin/env bash
set -euo pipefail
[[ -f .env ]] && { set -a; . ./.env; set +a; }

: "${PHP_SERVICE:=php81-apache}"
: "${DRUPAL_CONTAINER_PATH:=/var/www/miraweb2024}"

exec docker compose exec -T -w "$DRUPAL_CONTAINER_PATH" \
  "$PHP_SERVICE" composer --ansi "$@"

